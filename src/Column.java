import java.util.Iterator;

public class Column extends Stack {
    public Tile shiftUp(Tile newTile) {
        for (Iterator it = tiles.descendingIterator(); it.hasNext(); ) {
            Tile t = it.next();


        }
        return
    }
}
